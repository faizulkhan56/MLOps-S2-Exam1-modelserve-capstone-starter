# Grafana — file-based dashboards (Phase 7)
apiVersion: 1

providers:
  - name: ModelServe
    type: file
    orgId: 1
    folder: ""
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
