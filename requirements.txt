# ModelServe — Python dependencies (Phases 1–2: stack + API bootstrap)
# Pin for reproducibility; align with Dockerfile python:3.10-slim

fastapi>=0.104.0,<0.115.0
uvicorn[standard]>=0.24.0,<0.32.0
python-dotenv>=1.0.0,<2.0.0
prometheus-client>=0.19.0,<0.22.0

# Reserved for Phases 3+ (uncomment or add when those phases are implemented):
# mlflow>=2.10.0
# pandas>=2.0.0
# numpy>=1.24.0
# scikit-learn>=1.3.0
# feast[redis]>=0.34.0
# httpx>=0.25.0
# pytest>=7.4.0
# pytest-cov>=4.1.0
