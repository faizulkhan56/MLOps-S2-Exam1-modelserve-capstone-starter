# ModelServe — app + training + Feast (Phases 1–4)
# Python 3.10. Install: pip install -r requirements.txt

# API (Phases 1–2+)
fastapi>=0.104.0,<0.115.0
uvicorn[standard]>=0.24.0,<0.32.0
python-dotenv>=1.0.0,<2.0.0
prometheus-client>=0.19.0,<0.22.0

# Training + MLflow (Phase 3)
mlflow>=2.10.0,<2.17.0
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0,<1.6.0
pyarrow>=14.0.0,<16.0.0

# Feast (Phase 4)
feast[redis]>=0.36.0,<0.40.0

# Tests (Phase 6+)
httpx>=0.25.0
pytest>=7.4.0
